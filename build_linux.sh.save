#!/bin/bash

c++=g++ cd ./Consor

output="linux_build/consor"

files=`find ./ -name "*.cpp" | grep -v "Windows"`
g++-4.8 -g -std=c++11 -pthread $files -o ../$output -lboost_locale

#objs=`find ./ -name "*.o"`
#rm $objs

cd -

#c++ 
